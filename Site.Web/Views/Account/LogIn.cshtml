@model LogInViewModel

<main class="client-page">
    <div class="container-content">
        @*@if (ViewBag.Issuccess!=null)
        {
            <div class="alert alert-success">
                <p class="help-block">ورود با موفقیت انجام شد</p>
            </div>

            <script>
                // Your delay in milliseconds
                var delay = 3000;
                window.setTimeout(function () { window.location ="https://localhost:5001/"; }, delay);
            </script>
        }*@
        <header><h2> ورود به سایت </h2></header>


        <div class="form-layer">

            <form asp-action="LogIn" method="post">
                <div class="input-group">
                    <span class="input-group-addon" id="email-address"><i class="zmdi zmdi-email"></i></span>
                    <input asp-for="Email" type="email" class="form-control" placeholder="ایمیل" required>
                </div>
                <span asp-validation-for="Email" class="text-danger"></span>

                <div class="input-group">
                    <span class="input-group-addon" id="password"><i class="zmdi zmdi-lock"></i></span>
                    <input asp-for="PassWord" type="password" class="form-control" placeholder="رمز عبور " required>
                </div>
                <span asp-validation-for="PassWord" class="text-danger"></span>

                <div class="remember-me">
                    <label>  مرا بخاطر بسپار </label>
                    <input asp-for="RememberMe" type="checkbox">
                </div>
                <div class="input-group">
                    <div asp-validation-summary="All" class="text-danger"></div>
                </div>
                <div class="link">
                    <a asp-action="CheckEmail" asp-controller="Account"> <i class="zmdi zmdi-lock"></i> رمز عبور خود را فراموش کرده ام !</a>
                    <a asp-action="Register" asp-controller="Account" asp-route-returnUrl="@ViewContext.HttpContext.Request.PathAndQuery()"> <i class="zmdi zmdi-account"></i> عضویت در سایت </a>
                </div>
                <div class="input-group">
                    <input type="submit" asp-route-returnUrl="@ViewContext.HttpContext.Request.PathAndQuery()" class="btn btn-success" value="ورود به سایت " />
                </div>
            </form>
        </div>

    </div>
</main>